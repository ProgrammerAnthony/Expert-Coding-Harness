# 同会话执行计划（无子代理时）

当环境**无法**派发 Task/子代理时，由**当前会话**按实施计划中的 `- [ ]` 逐步执行：

1. 严格执行 **TDD 开发大师**：先失败测试、再最小实现、再重构。
2. 每完成一个**任务**（或每 N 个步骤），与用户做一次同步或运行本地测试确认。
3. 在合并或认为完成前，至少调用一次 `code-review-expert（代码审查专家）` 对 `git diff` 范围做正式审查。

此路径**不**称为「子代理驱动开发」；仅作为能力降级时的执行方式说明。
