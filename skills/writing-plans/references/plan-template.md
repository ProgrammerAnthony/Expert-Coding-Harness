# [功能名] 实施计划（模板）

> **给执行者：** 任务使用 `- [ ]` 勾选跟踪；每步必须写清楚路径、命令、预期输出与验收标准。

**目标：**（一句话说明交付什么）

**非目标（Out of Scope）：**（明确不做什么，避免范围蔓延）

**架构要点：**（2–5 句，说明关键边界/数据流/依赖）

**技术栈/运行方式：**（语言/框架/测试命令/启动命令）

**关联设计文档：** `docs/...`（如有）

---

## 文件变更清单（先写这个再拆任务）

- 新建：
  - `path/to/new_file.ext`（职责一句话）
- 修改：
  - `path/to/existing.ext`（为什么改、影响面）
- 测试：
  - `path/to/test.ext`（覆盖哪些验收标准）

---

## 任务列表（可勾选执行）

### 任务 1：[主题名]

**涉及文件：**
- 新建：`...`
- 修改：`...`
- 测试：`...`

- [ ] **步骤 1：编写失败测试**
  - 代码块：（贴出测试代码）
- [ ] **步骤 2：运行并确认失败**
  - 运行：`...`
  - 预期：失败信息（写明关键断言/错误）
- [ ] **步骤 3：最小实现**
  - 代码块：（贴出实现代码或 diff 级说明）
- [ ] **步骤 4：运行并确认通过**
  - 运行：`...`
  - 预期：通过
- [ ] **步骤 5：提交（可选，按项目约定）**
  - `git status -sb`
  - `git diff`
  - `git commit -m "..."`

---

## 质量门控（执行前/执行中必须遵守）

- [ ] 不允许 `TBD/TODO/稍后再说/适当处理` 这类占位词出现在可执行步骤中
- [ ] 每个步骤都必须可在本地运行（有命令）且可验证（有预期输出）
- [ ] 任何需要用户确认的决策点必须显式列出并暂停等待
